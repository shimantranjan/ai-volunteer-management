# This directory is intended for ORM models.
# Since we are using motor for MongoDB natively, document mapping is dynamic.
# The Pydantic models in the schemas/ directory handle validation and serialization.
